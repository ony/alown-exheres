# Copyright 2012 Ali Lwon
# Distributed under the terms of the GNU General Public License v2

SLOT="1.7"
require java [ virtual_jdk_and_jre_deps=false ] java-jdk

SUMMARY="Oracle Binary JDK"
DESCRIPTION="
Installs the Oracle provided JDK binaries
"
HOMEPAGE="http://www.oracle.com/technetwork/java/index.html"
DOWNLOADS="http://download.oracle.com/otn-pub/java/jdk/7u9-b05/jdk-7u9-linux-x64.tar.gz"

LICENCES="Oracle"
PLATFORMS="~amd64"
MYOPTIONS=""

DEPENDENCIES="
    build:
    build+run:
        !dev-lang/icedtea7:1.7
"

BUGS_TO="ali@lown.me.uk"

DEFAULT_SRC_CONFIGURE_PARAMS=( )
DEFAULT_SRC_CONFIGURE_OPTION_WITHS=( )
DEFAULT_SRC_CONFIGURE_OPTION_ENABLES=( )

WORK="${WORKBASE}/jdk1.7.0_09"

src_install() {
    local java_home="/usr/${LIBDIR}/${PN}"
    dodir "${java_home}"

    edo cd "${build_target}"

    echo "Put into:"
    echo $java_home
    echo "---"

    echo "Instinto"
    insinto "${java_home}"
    echo "Finshed insinto"
    doins -r bin include jre lib man

    echo "CHModd"
    edo chmod 755 "${IMAGE}/${java_home}"/{,jre/}bin/*

    echo "Finished insing"

    linkify_redundant_jdk_bins
    echo "Finished linking"
    do_jdk_alternatives
    echo "Finished setup alternatives"
}

